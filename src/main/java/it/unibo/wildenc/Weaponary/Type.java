package it.unibo.wildenc.Weaponary;

/**
 * Enumeration for the types of the moves. Each type changes the name and
 * the appearence of the generated projectiles.
 */
public enum Type {
    FIRE,
    WATER,
    GRASS
}
